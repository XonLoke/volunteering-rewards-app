# ─── Docker / Root-level env variables ───────────────────
# Backend configuration is in backend/.env
# This file is for Docker Compose or CI use only.

# Database (used by docker-compose.yml)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_db_password_here
POSTGRES_DB=volunteering_rewards
